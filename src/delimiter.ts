/**
 * A unique delimiter string used to separate or identify tokens in serialized data.
 * @example
 * `Hello${delimiter}World`.split(delimiter)  // ["Hello", "World"]
 */
export const delimiter = "{{DELIMITER}}";
